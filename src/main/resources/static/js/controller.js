export default {
    async ordersRoute() {
        //call the api
        console.log("i need to call the api with orders info")
    },
    async profileRoute() {
        //call the api
        console.log("i need to call the profile api to load the info about the user")
    }
}