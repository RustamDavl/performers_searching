spring:
 datasource:
  url: jdbc:postgresql://localhost:5432/Level
  username: postgres
  password: password
  driver-class-name: org.postgresql.Driver
 jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
    open-in-view: false
logging:
  level:
    root: warn
  file:
    name: rustdv.log
    path: /


